public interface ModuleComponent extends CourseComponent
{
    // Extends CourseComponent to maintain the same interface
    // Used to ensure decorators implement the same contract as Module
    // ekta wrapper interface banano laglo just to maintain the decorated pattern principle
}
